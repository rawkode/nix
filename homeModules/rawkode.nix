# Main rawkode home configuration module that combines all others
{ config, lib, pkgs, ... }:
{
  # This module combines all the separate modules
  # Each is now a standalone flakelight module that can be used independently
}
